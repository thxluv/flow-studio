# Архив

Сюда перенесены файлы, **не нужные для работы и публикации** сайта:

- внутренние планы и статусы (`.txt`)
- локальные скрипты деплоя (`.bat`, `.ps1`)
- дубликат `render.yaml` из `flowphoto-server/`

Код приложений не трогали — только уборка корня репозитория.